server:
  port: 0

spring:
  application:
    name: review

  datasource:
    url: jdbc:postgresql://ep-rough-rice-a8g9ge26-pooler.eastus2.azure.neon.tech/neondb?sslmode=require
    username: neondb_owner
    password: npg_9s5uyCLdNARI
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

security:
  jwt:
    secret-key: "mysupersecretkeyforjwtsecurity123456"
    expiration-time: 86400000

springdoc:
  swagger-ui:
    path: /swagger-ui.html
    try-it-out-enabled: true
    display-request-duration: true

eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: https://eurekaserver-lzph.onrender.com/eureka
  instance:
    prefer-ip-address: true

azure:
  endpoint: ${AZURE_ENDPOINT}
  key: ${AZURE_KEY}
